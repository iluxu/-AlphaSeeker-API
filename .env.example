# OpenAI API Key (Required for GPT features)
# Get yours from https://platform.openai.com/account/api-keys
OPENAI_API_KEY="your_openai_api_key_here"

# Uvicorn Server Configuration (Optional, defaults will be used otherwise)
HOST="127.0.0.1"
PORT=8029
# LOG_LEVEL="INFO" # Can be DEBUG, INFO, WARNING, ERROR, CRITICAL (default set in script)
# UVICORN_RELOAD="false" # Set to "true" for development auto-reload
